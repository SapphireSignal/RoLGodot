# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Frozen.dws (CLIENT).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Frozen.dws:3
func ApplyWithDuration(Entity, Duration: int) -> void:
	var Group: int = 0  # 4
	var ImmuneGroup: int = 0  # 4
	var SoundKey: String = ""  # 6
	Group = Entity.ReserveFreeGroup()  # 9
	ImmuneGroup = Entity.ReserveFreeGroup()  # 10
	Entity.Blackboard.SetValue(C.eiCooldown, [Group], Duration)  # 11
	Entity.Blackboard.SetValue(C.eiCooldown, [ImmuneGroup], Duration + 10000)  # 12
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upHasStateEffect, C.upFrozen])  # 38
	TUnitPropertyComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.upImmuneToFrozen])  # 39
	TParticleEffectComponent.new().CreateGrouped(Entity, [Group], "\\Shared\\Frozen.pfx", 1.0).ActivateNow().ActivateOnCreate().ScaleWith(C.eiCollisionRadius)  # 42
	TMeshEffectComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([]).SetEffect(TMeshEffectIce.new().Create(Duration).AddKey(0, 0.0).AddKey(500, 1.0).AddKey(Duration - 250, 1.0).AddKey(Duration, 0.0))  # 46
	if Entity.CollisionRadius > 0.7:  # 57
		SoundKey = "event:/card/black/spell/frostspear/freeze_big"  # 58
	else:
		SoundKey = "event:/card/black/spell/frostspear/freeze_small"  # 60
	TSoundComponent.new().CreateGrouped(Entity, [Group], SoundKey).TriggerNow()  # 61

# Modifiers\Frozen.dws:66
func Apply(Entity) -> void:
	ApplyWithDuration(Entity, 9000)  # 68
