# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Drop.dws (CLIENT).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Drop.dws:3
func Apply(Entity) -> void:
	var ColorIdentity: int = 0  # 5
	ColorIdentity = Entity.ColorIdentity()  # 12
	if ColorIdentity == C.ecGreen:  # 13
		TParticleEffectComponent.new().CreateGrouped(Entity, [0], "\\Green\\drop_green.pfx", 2.0).IgnoreModelSize().ActivateNow().FixedHeightGround()  # 15
	elif ColorIdentity == C.ecBlack:  # 20
		TParticleEffectComponent.new().CreateGrouped(Entity, [0], "\\Black\\drop_black.pfx", 2.0).ActivateNow().BindToSubPosition(C.BIND_ZONE_GROUND)  # 22
	elif ColorIdentity == C.ecBlue:  # 26
		TParticleEffectComponent.new().CreateGrouped(Entity, [0], "\\Blue\\drop_blue.pfx", 2.0).ActivateNow().BindToSubPosition(C.BIND_ZONE_TOP)  # 28
	elif ColorIdentity == C.ecColorless:  # 32
		TParticleEffectComponent.new().CreateGrouped(Entity, [0], "\\Colorless\\drop_colorless.pfx", 0.5).IgnoreModelSize().ScaleWith(C.eiCollisionRadius).ScaleRange(0.55, 0.7).FixedHeightGround().ActivateNow()  # 34
	else:
		TParticleEffectComponent.new().CreateGrouped(Entity, [0], "\\White\\drop_white.pfx", 10.0).ActivateNow().BindToSubPositionGroup(C.BIND_ZONE_TOP, [0, 1])  # 44
	TMeshEffectSpawn.new().Create().AssignToEntity(Entity)  # 48
