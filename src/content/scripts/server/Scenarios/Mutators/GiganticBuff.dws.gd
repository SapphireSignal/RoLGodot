# Generated by tools/transpile_scripts.py from Scripts\Scenarios\Mutators\GiganticBuff.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Scenarios\Mutators\GiganticBuff.dws:3
func Apply(Entity, League: int) -> void:
	var Group: int = 0  # 4
	var HealthGroup: int = 0  # 4
	Group = Entity.ReserveFreeGroup()  # 6
	HealthGroup = Entity.ReserveFreeGroup()  # 7
	Entity.Blackboard.SetValue(C.eiWelaModifier, [Group], 1.75)  # 8
	Entity.Blackboard.SetValue(C.eiWelaDamage, [HealthGroup], 165.0)  # 9
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btPositive, C.btDivine])  # 12
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group, HealthGroup]).TargetGroup([Group, HealthGroup])  # 13
	TModifierResourceComponent.new().CreateGrouped(Entity, [HealthGroup]).Resource(C.reHealth).ApplyNow()  # 17
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upBlessedGrowth])  # 22
	TModifierWelaDamageComponent.new().CreateGrouped(Entity, [1, Group]).Multiply().SetValueGroup([Group])  # 25
