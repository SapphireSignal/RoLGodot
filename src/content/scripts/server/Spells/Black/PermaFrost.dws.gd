# Generated by tools/transpile_scripts.py from Scripts\Spells\Black\PermaFrost.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Spells\Black\PermaFrost.dws:1
func Apply(Entity, SameTeam: bool) -> void:
	var Group: int = 0  # 2
	var ArmorGroup: int = 0  # 2
	Group = Entity.ReserveFreeGroup()  # 4
	ArmorGroup = Entity.ReserveFreeGroup()  # 5
	Entity.Blackboard.SetValue(C.eiWelaDamage, [Group], 300.0)  # 6
	if not SameTeam:  # 8
		Entity.Blackboard.SetValue(C.eiWelaModifier, [Group], -1.0)  # 9
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btPositive])  # 12
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([Group])  # 13
	if SameTeam:  # 16
		TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Permafrost").TriggerOnTakeDamage()  # 17
	TModifierResourceComponent.new().CreateGrouped(Entity, [Group]).Resource(C.reHealth).ApplyNow()  # 21
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upBlessed, C.upBlessedHardening])  # 26
	TWelaReadyUnitPropertyComponent.new().CreateGrouped(Entity, [ArmorGroup]).MustHave([C.upFrozen])  # 28
	TModifierArmorTypeComponent.new().CreateGrouped(Entity, [ArmorGroup]).SetTo(C.atHeavy).ReadyGroup([ArmorGroup])  # 30
