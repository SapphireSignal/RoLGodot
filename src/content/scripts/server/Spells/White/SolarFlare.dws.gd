# Generated by tools/transpile_scripts.py from Scripts\Spells\White\SolarFlare.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Spells\White\SolarFlare.dws:3
func Apply(Entity, League: int) -> void:
	var EffectGroup: int = 0  # 4
	var DurationGroup: int = 0  # 4
	EffectGroup = Entity.ReserveFreeGroup()  # 6
	DurationGroup = Entity.ReserveFreeGroup()  # 7
	Entity.Blackboard.SetValue(C.eiCooldown, [DurationGroup], 10000)  # 8
	Entity.Blackboard.SetValue(C.eiWelaRange, [EffectGroup], 8.0)  # 10
	Entity.Blackboard.SetValue(C.eiCooldown, [EffectGroup], 200)  # 11
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [DurationGroup]).ThinksPassively()  # 15
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [DurationGroup], [C.btPositive])  # 17
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [DurationGroup], false)  # 18
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [DurationGroup]).TargetGroup([DurationGroup, EffectGroup])  # 19
	TThinkImpulseImmediateComponent.new().CreateGrouped(Entity, [EffectGroup])  # 23
	TBrainWelaFightComponent.new().CreateGrouped(Entity, [EffectGroup]).DisableTargetLock().ThinksPassively()  # 24
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [EffectGroup], false).Cooldown(3000).Once()  # 27
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [EffectGroup], true)  # 30
	TWelaTargetingRadialComponent.new().CreateGrouped(Entity, [EffectGroup]).SetTargetTeamConstraint(C.tcAllies).PicksRandomTargets()  # 31
	TWelaTargetConstraintAlliesComponent.new().CreateGrouped(Entity, [EffectGroup])  # 34
	TWelaTargetConstraintNotSelfComponent.new().CreateGrouped(Entity, [EffectGroup])  # 35
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [EffectGroup]).MustHave([C.upUnit]).MustNotHave([C.upUnhealable])  # 36
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [EffectGroup])  # 40
	TWarheadApplyScriptComponent.new().CreateGrouped(Entity, [EffectGroup], "Spells\\White\\SolarFlareSubFlare.dws").PassIntValue(League)  # 43
