# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Frozen.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Frozen.dws:3
func ApplyWithDuration(Entity, Duration: int) -> void:
	var Group: int = 0  # 4
	var ImmuneGroup: int = 0  # 4
	Group = Entity.ReserveFreeGroup()  # 9
	ImmuneGroup = Entity.ReserveFreeGroup()  # 10
	Entity.Blackboard.SetValue(C.eiCooldown, [Group], Duration)  # 11
	Entity.Blackboard.SetValue(C.eiCooldown, [ImmuneGroup], Duration + 10000)  # 12
	Entity.Eventbus.Trigger(C.eiStand, [])  # 16
	Entity.Eventbus.Trigger(C.eiWelaStop, [])  # 17
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [Group]).ThinksPassively()  # 19
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btNegative, C.btState])  # 21
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [Group], false)  # 22
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group])  # 23
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Frozen").TriggerOnKilled()  # 24
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [ImmuneGroup]).ThinksPassively()  # 28
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.btDivine])  # 30
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [ImmuneGroup], false)  # 31
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [ImmuneGroup])  # 32
	TWelaHelperBeaconComponent.new().CreateGrouped(Entity, [Group, ImmuneGroup]).TriggerAt([C.upFrozen])  # 34
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upHasStateEffect, C.upFrozen])  # 38
	TUnitPropertyComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.upImmuneToFrozen])  # 39

# Modifiers\Frozen.dws:66
func Apply(Entity) -> void:
	ApplyWithDuration(Entity, 9000)  # 68
