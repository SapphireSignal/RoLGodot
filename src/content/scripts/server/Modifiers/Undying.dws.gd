# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Undying.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Undying.dws:3
func Apply(Entity) -> void:
	var Group: int = 0  # 4
	var DieGroup: int = 0  # 4
	var HealthGroup: int = 0  # 4
	Group = Entity.ReserveFreeGroup()  # 6
	DieGroup = Entity.ReserveFreeGroup()  # 7
	HealthGroup = Entity.ReserveFreeGroup()  # 8
	Entity.Blackboard.SetValue(C.eiCooldown, [Group], 15000)  # 9
	Entity.Blackboard.SetValue(C.eiWelaDamage, [HealthGroup], 100000.0)  # 11
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [HealthGroup]).ThinksPassively()  # 15
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [HealthGroup])  # 17
	TWarheadSpottyResourceComponent.new().CreateGrouped(Entity, [HealthGroup]).SetResourceType(C.reHealth)  # 18
	TThinkImpulseNowComponent.new().CreateGrouped(Entity, [HealthGroup])  # 20
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [HealthGroup]).TargetGroup([HealthGroup])  # 21
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [Group]).ThinksPassively()  # 24
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btNegative])  # 26
	TWelaEffectSuicideComponent.new().CreateGrouped(Entity, [Group])  # 27
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([Group, DieGroup])  # 28
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Undying").TriggerOnKill()  # 30
	TAutoBrainOnDeathComponent.new().CreateGrouped(Entity, [DieGroup])  # 35
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upHasUndying, C.upUnhealable])  # 38
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [Group], false)  # 39
