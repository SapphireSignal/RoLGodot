# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Bleeding.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Bleeding.dws:1
func Apply(Entity) -> void:
	var Group: int = 0  # 2
	var BleedingGroup: int = 0  # 2
	Group = Entity.ReserveFreeGroup()  # 4
	BleedingGroup = Entity.ReserveFreeGroup()  # 5
	Entity.Blackboard.SetValue(C.eiCooldown, [Group], 10000)  # 6
	Entity.Blackboard.SetValue(C.eiWelaModifier, [Group], 0.60)  # 7
	Entity.Blackboard.SetIndexedValue(C.eiResourceBalance, [Group], C.reWelaCharge, 1)  # 8
	Entity.Blackboard.SetIndexedValue(C.eiResourceCap, [Group], C.reWelaCharge, 20)  # 9
	Entity.Blackboard.SetValue(C.eiCooldown, [BleedingGroup], 1000)  # 10
	Entity.Blackboard.SetValue(C.eiWelaDamage, [BleedingGroup], 0.005)  # 11
	Entity.Blackboard.SetValue(C.eiDamageType, [BleedingGroup], [C.dtAbility, C.dtDoT])  # 12
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [Group]).ThinksPassively()  # 16
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btNegative, C.btState])  # 18
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [Group], false)  # 19
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([Group, BleedingGroup])  # 20
	TWelaHelperBeaconComponent.new().CreateGrouped(Entity, [Group]).TriggerAt([C.upBleeding])  # 23
	TBuffTakenDamageMultiplierComponent.new().CreateGrouped(Entity, [Group]).ApplyOnHeal()  # 26
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [BleedingGroup]).ThinksPassively()  # 29
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [BleedingGroup], false)  # 31
	TWelaTargetConstraintEventComponent.new().CreateGrouped(Entity, [BleedingGroup], C.eiDamageable)  # 32
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [BleedingGroup]).MustNotHave([C.upBanished])  # 33
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [BleedingGroup])  # 35
	TWarheadSpottyDamageComponent.new().CreateGrouped(Entity, [BleedingGroup]).PercentageOfMaxHealth()  # 36
	TModifierWelaDamageComponent.new().CreateGrouped(Entity, [BleedingGroup]).ScaleWithResource(C.reWelaCharge).ResourceGroup([Group]).Multiply()  # 39
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upHasStateEffect, C.upBleeding])  # 45
