# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Blind.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Blind.dws:3
func Apply(Entity) -> void:
	var BLIND_DURATION: int = 12000  # 4
	var BLIND_IMMUNITY_DURATION: int = 10000  # 5
	var Group: int = 0  # 6
	var BlindGroup: int = 0  # 6
	var ImmuneGroup: int = 0  # 6
	Group = Entity.ReserveFreeGroup()  # 8
	BlindGroup = Entity.ReserveFreeGroup()  # 9
	ImmuneGroup = Entity.ReserveFreeGroup()  # 10
	Entity.Blackboard.SetValue(C.eiCooldown, [Group], BLIND_DURATION)  # 11
	Entity.Blackboard.SetValue(C.eiCooldown, [ImmuneGroup], BLIND_DURATION + BLIND_IMMUNITY_DURATION)  # 12
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [Group]).ThinksPassively()  # 16
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btNegative, C.btState])  # 18
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [Group], false)  # 19
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([Group, BlindGroup])  # 20
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Blind").TriggerOnKilled()  # 22
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [ImmuneGroup]).ThinksPassively()  # 27
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.btDivine])  # 29
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [ImmuneGroup], false)  # 30
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [ImmuneGroup])  # 31
	TModifierBlindedComponent.new().CreateGrouped(Entity, [1, Group]).SetFireGroup([BlindGroup]).SetValueGroup([1])  # 34
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group, BlindGroup], [C.upHasStateEffect, C.upBlinded])  # 39
	TUnitPropertyComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.upImmuneToBlinded])  # 40
