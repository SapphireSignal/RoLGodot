# Generated by tools/transpile_scripts.py from Scripts\Modifiers\BlessingHealth.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\BlessingHealth.dws:3
func Apply(Entity) -> void:
	var HealthGroup: int = 0  # 4
	var Group: int = 0  # 4
	Group = Entity.ReserveFreeGroup()  # 6
	HealthGroup = Entity.ReserveFreeGroup()  # 7
	Entity.Blackboard.SetValue(C.eiWelaDamage, [HealthGroup], 0.3)  # 8
	Entity.Blackboard.SetValue(C.eiWelaModifier, [HealthGroup], 50.0)  # 9
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btPositive])  # 12
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([Group, HealthGroup])  # 13
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Enchanted").TriggerOnTakeDamage()  # 16
	TModifierResourceComponent.new().CreateGrouped(Entity, [HealthGroup]).Resource(C.reHealth).ScaleWithResource(C.reHealth).UseResourceCap().AddModifier().ApplyNow()  # 21
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upBlessed, C.upBlessedHealth])  # 29
