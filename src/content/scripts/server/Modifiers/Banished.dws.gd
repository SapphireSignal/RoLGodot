# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Banished.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Banished.dws:3
func Apply(Entity) -> void:
	var Group: int = 0  # 4
	var ImmuneGroup: int = 0  # 4
	Group = Entity.ReserveFreeGroup()  # 6
	ImmuneGroup = Entity.ReserveFreeGroup()  # 7
	Entity.Blackboard.SetValue(C.eiCooldown, [Group], 10000)  # 8
	Entity.Blackboard.SetValue(C.eiCooldown, [ImmuneGroup], 10000 + 10000)  # 9
	Entity.Eventbus.Trigger(C.eiStand, [])  # 13
	Entity.Eventbus.Trigger(C.eiWelaStop, [])  # 14
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [Group]).ThinksPassively()  # 16
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btNegative, C.btState])  # 18
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [Group], false)  # 19
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group])  # 20
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Banished").TriggerOnKilled()  # 21
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [ImmuneGroup]).ThinksPassively()  # 25
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.btDivine])  # 27
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [ImmuneGroup], false)  # 28
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [ImmuneGroup])  # 29
	TWelaHelperBeaconComponent.new().CreateGrouped(Entity, [Group, ImmuneGroup]).TriggerAt([C.upBanished])  # 31
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upHasStateEffect, C.upBanished])  # 35
	TUnitPropertyComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.upImmuneToBanished])  # 36
