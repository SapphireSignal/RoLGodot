# Generated by tools/transpile_scripts.py from Scripts\Modifiers\Root.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\Root.dws:3
func Apply(Entity) -> void:
	var ROOT_DURATION: int = 12000  # 4
	var Group: int = 0  # 5
	var DoTGroup: int = 0  # 5
	var ImmuneGroup: int = 0  # 5
	Group = Entity.ReserveFreeGroup()  # 7
	DoTGroup = Entity.ReserveFreeGroup()  # 8
	ImmuneGroup = Entity.ReserveFreeGroup()  # 9
	Entity.Blackboard.SetValue(C.eiCooldown, [Group], ROOT_DURATION)  # 10
	Entity.Blackboard.SetValue(C.eiCooldown, [DoTGroup], 1000)  # 11
	Entity.Blackboard.SetValue(C.eiWelaDamage, [DoTGroup], 1.5)  # 12
	Entity.Blackboard.SetValue(C.eiDamageType, [DoTGroup], [C.dtSpell, C.dtAbility, C.dtDoT])  # 13
	Entity.Blackboard.SetValue(C.eiCooldown, [ImmuneGroup], ROOT_DURATION + 10000)  # 14
	Entity.Eventbus.Trigger(C.eiStand, [])  # 18
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [Group]).ThinksPassively()  # 20
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btNegative, C.btState])  # 22
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [Group], false)  # 23
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([Group, DoTGroup])  # 24
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Root").TriggerOnKilled()  # 26
	TThinkImpulseTimerCooldownComponent.new().CreateGrouped(Entity, [DoTGroup]).TimerIsReady()  # 31
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [DoTGroup]).ThinksLocal().ThinksPassively()  # 33
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [DoTGroup], true)  # 36
	TWelaReadyNthComponent.new().CreateGrouped(Entity, [DoTGroup]).Times(12)  # 37
	TWelaTargetConstraintEventComponent.new().CreateGrouped(Entity, [DoTGroup], C.eiDamageable)  # 39
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [DoTGroup])  # 40
	TWarheadSpottyDamageComponent.new().CreateGrouped(Entity, [DoTGroup])  # 41
	TBrainWelaSelftargetComponent.new().CreateGrouped(Entity, [ImmuneGroup]).ThinksPassively()  # 44
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.btDivine])  # 46
	TWelaReadyCooldownComponent.new().CreateGrouped(Entity, [ImmuneGroup], false)  # 47
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [ImmuneGroup])  # 48
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upHasStateEffect, C.upRooted])  # 51
	TUnitPropertyComponent.new().CreateGrouped(Entity, [ImmuneGroup], [C.upImmuneToRooted])  # 52
