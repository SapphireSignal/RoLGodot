# Generated by tools/transpile_scripts.py from Scripts\Modifiers\BlessingStrength.dws (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Modifiers\BlessingStrength.dws:3
func Apply(Entity) -> void:
	var DamageGroup: int = 0  # 4
	var HealthGroup: int = 0  # 4
	var Group: int = 0  # 4
	Group = Entity.ReserveFreeGroup()  # 6
	HealthGroup = Entity.ReserveFreeGroup()  # 7
	DamageGroup = Entity.ReserveFreeGroup()  # 8
	Entity.Blackboard.SetValue(C.eiWelaDamage, [HealthGroup], 40.0)  # 9
	Entity.Blackboard.SetValue(C.eiWelaModifier, [DamageGroup], 8.0)  # 10
	TAutoBrainBuffComponent.new().CreateGrouped(Entity, [Group], [C.btPositive])  # 13
	TWelaEffectRemoveAfterUseComponent.new().CreateGrouped(Entity, [Group]).TargetGroup([Group, DamageGroup, HealthGroup])  # 14
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("Enchanted").TriggerOnTakeDamage()  # 17
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [Group]).Name("EnchantedStrength").TriggerOnDamageDone()  # 21
	TModifierResourceComponent.new().CreateGrouped(Entity, [HealthGroup]).Resource(C.reHealth).ApplyNow()  # 26
	TUnitPropertyComponent.new().CreateGrouped(Entity, [Group], [C.upBlessed, C.upBlessedStrength])  # 31
	TModifierWelaDamageComponent.new().CreateGrouped(Entity, [0, 1, Group]).FactorForUnitProperty([C.upLinkWeapon], 0.5).SetValueGroup([DamageGroup])  # 33
