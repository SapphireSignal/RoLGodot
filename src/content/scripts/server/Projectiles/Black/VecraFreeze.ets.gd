# Generated by tools/transpile_scripts.py from Scripts\Projectiles\Black\VecraFreeze.ets (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Projectiles\Black\VecraFreeze.ets:3
func CreateData(Entity) -> void:
	Entity.Blackboard.SetValue(C.eiWelaRange, [0], 8.0)  # 5
	Entity.Blackboard.SetValue(C.eiWelaTargetCount, [0], 200)  # 6
	Entity.Blackboard.SetValue(C.eiWelaAreaOfEffect, [1], 8.0)  # 8
	Entity.Blackboard.SetValue(C.eiWelaDamage, [1], 1000.0)  # 9
	Entity.Blackboard.SetValue(C.eiDamageType, [1], [C.dtSpell, C.dtSplash])  # 10

# Projectiles\Black\VecraFreeze.ets:13
func CreateMeta(Entity) -> void:
	CreateData(Entity)  # 15

# Projectiles\Black\VecraFreeze.ets:21
func CreateEntity(Entity) -> void:
	CreateMeta(Entity)  # 23
	TThinkImpulseOnceComponent.new().CreateGrouped(Entity, [1])  # 26
	TBrainWelaSelftargetGroundComponent.new().CreateGrouped(Entity, [1])  # 28
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [1]).MustHave([C.upUnit, C.upFrozen]).MustNotHave([C.upBanished])  # 29
	TWelaTargetConstraintEnemiesComponent.new().CreateGrouped(Entity, [1])  # 32
	TWelaTargetingRadialComponent.new().CreateGrouped(Entity, [1]).SetTargetTeamConstraint(C.tcEnemies)  # 33
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [1])  # 35
	TWarheadSplashDamageComponent.new().CreateGrouped(Entity, [1]).TargetsGroundAndAir()  # 36
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [1]).Name("Splash").TriggerOnKillDone()  # 38
	TThinkImpulseOnceComponent.new().CreateGrouped(Entity, [0])  # 42
	TBrainWelaFightComponent.new().CreateGrouped(Entity, [0])  # 44
	TWelaTargetingRadialComponent.new().CreateGrouped(Entity, [0]).SetTargetTeamConstraint(C.tcEnemies)  # 45
	TWelaTargetConstraintEnemiesComponent.new().CreateGrouped(Entity, [0])  # 47
	TWelaTargetConstraintEventComponent.new().CreateGrouped(Entity, [0], C.eiDamageable)  # 48
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [0]).MustHave([C.upUnit]).MustNotHave([C.upFrozen, C.upImmuneToFrozen, C.upLegendary, C.upImmuneToStateEffects])  # 49
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [0])  # 52
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [0]).Name("Frozen").Name("IcyPrison").TriggerOnFire()  # 53
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [0]).Name("FrozenRanged").CheckUnitPropertyMustHave([C.upRanged]).TriggerOnFire()  # 57
	TWelaEffectSuicideComponent.new().CreateGrouped(Entity, [0])  # 62
	TWarheadApplyScriptComponent.new().CreateGrouped(Entity, [0], "Modifiers\\Frozen.dws")  # 65
