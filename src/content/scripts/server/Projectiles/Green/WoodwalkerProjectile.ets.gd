# Generated by tools/transpile_scripts.py from Scripts\Projectiles\Green\WoodwalkerProjectile.ets (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Projectiles\Green\WoodwalkerProjectile.ets:3
func CreateData(Entity) -> void:
	Entity.Blackboard.SetValue(C.eiSpeed, [], float(20) / 1000)  # 5

# Projectiles\Green\WoodwalkerProjectile.ets:8
func CreateMeta(Entity) -> void:
	CreateData(Entity)  # 10

# Projectiles\Green\WoodwalkerProjectile.ets:37
func CreateEntity(Entity) -> void:
	CreateMeta(Entity)  # 39
	TMovementComponent.new().Create(Entity)  # 42
	TBrainProjectileComponent.new().CreateGrouped(Entity, [0])  # 43
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [0])  # 45
	TWarheadSpottyDamageComponent.new().CreateGrouped(Entity, [0])  # 46
