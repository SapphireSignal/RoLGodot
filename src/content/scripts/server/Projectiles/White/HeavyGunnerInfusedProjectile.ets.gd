# Generated by tools/transpile_scripts.py from Scripts\Projectiles\White\HeavyGunnerInfusedProjectile.ets (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")

var InheritsFrom: String = "Projectiles/White/HeavyGunnerBaseProjectile.ets"  # Projectiles\White\HeavyGunnerInfusedProjectile.ets:2

# Projectiles\White\HeavyGunnerInfusedProjectile.ets:4
func CreateData(Entity) -> void:
	Entity.Blackboard.SetValue(C.eiWelaRange, [1, 2, 3, 4], 3.5)  # 6
	Entity.Blackboard.SetValue(C.eiWelaTargetCount, [1, 2, 3, 4], 200)  # 7

# Projectiles\White\HeavyGunnerInfusedProjectile.ets:10
func CreateMeta(Entity) -> void:
	CreateData(Entity)  # 12

# Projectiles\White\HeavyGunnerInfusedProjectile.ets:18
func CreateEntity(Entity) -> void:
	CreateMeta(Entity)  # 20
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [0]).Name("Lightgrenades").TriggerOnDamageDoneTargets()  # 23
	TWelaEffectFireComponent.new().CreateGrouped(Entity, [0]).TargetGroup([1])  # 27
	TThinkImpulseFireComponent.new().CreateGrouped(Entity, [1]).TargetGroup([2])  # 29
	TBrainWelaFightComponent.new().CreateGrouped(Entity, [2]).ThinksLocal()  # 32
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [2]).MustHave([C.upUnit, C.upGround]).MustNotHave([C.upLegendary, C.upNoAutoAttack, C.upImmuneToBlinded, C.upImmuneToStateEffects])  # 34
	TWelaTargetConstraintEnemiesComponent.new().CreateGrouped(Entity, [2])  # 37
	TWelaTargetingRadialComponent.new().CreateGrouped(Entity, [2]).SetTargetTeamConstraint(C.tcEnemies)  # 38
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [2])  # 40
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [2]).Name("Blind").TriggerOnFire()  # 41
	TWelaEffectFireComponent.new().CreateGrouped(Entity, [0]).TargetGroup([3])  # 45
	TThinkImpulseFireComponent.new().CreateGrouped(Entity, [3]).TargetGroup([4])  # 47
	TBrainWelaFightComponent.new().CreateGrouped(Entity, [4]).ThinksLocal()  # 50
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [4]).MustHave([C.upUnit, C.upGround]).MustNotHave([C.upLegendary, C.upImmuneToStateEffects])  # 52
	TWelaTargetConstraintEnemiesComponent.new().CreateGrouped(Entity, [4])  # 55
	TWelaTargetingRadialComponent.new().CreateGrouped(Entity, [4]).SetTargetTeamConstraint(C.tcEnemies)  # 56
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [4])  # 58
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [4]).Name("Stun").TriggerOnFire()  # 59
	TWarheadApplyScriptComponent.new().CreateGrouped(Entity, [2], "Modifiers\\Blind.dws")  # 64
	TWarheadApplyScriptComponent.new().CreateGrouped(Entity, [4], "Modifiers\\Stun.dws")  # 65
