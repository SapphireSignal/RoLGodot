# Generated by tools/transpile_scripts.py from Scripts\Projectiles\Blue\Missile.ets (SERVER).
# Do not edit: change the transpiler and regenerate.
extends RefCounted

const C = preload("res://src/runtime/dws/dws_const.gd")
const L = preload("res://src/runtime/dws/dws_lib.gd")


# Projectiles\Blue\Missile.ets:3
func CreateData(Entity) -> void:
	Entity.Blackboard.SetValue(C.eiSpeed, [], float(20) / 1000)  # 5
	Entity.Blackboard.SetValue(C.eiWelaModifier, [1], 2.0)  # 8

# Projectiles\Blue\Missile.ets:11
func CreateMeta(Entity) -> void:
	var Color_: int = 0  # 12
	CreateData(Entity)  # 14

# Projectiles\Blue\Missile.ets:67
func CreateEntity(Entity) -> void:
	CreateMeta(Entity)  # 69
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, []).Name("Missile").TriggerOnCreate()  # 72
	TMovementComponent.new().Create(Entity)  # 76
	TBrainProjectileComponent.new().CreateGrouped(Entity, [0]).NoTargetChecks()  # 77
	TWelaTargetConstraintEnemiesComponent.new().CreateGrouped(Entity, [0])  # 79
	TWelaTargetConstraintEventComponent.new().CreateGrouped(Entity, [0], C.eiDamageable)  # 80
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [0]).MustNotHave([C.upBanished])  # 81
	TWelaEffectInstantComponent.new().CreateGrouped(Entity, [0])  # 83
	TWarheadSplashDamageComponent.new().CreateGrouped(Entity, [0])  # 84
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [0]).Name("Splash").TriggerOnKillDone()  # 85
	TWelaEffectStatisticsComponent.new().CreateGrouped(Entity, [0]).Name("AntiAir").TriggerOnDamageDone().CheckUnitPropertyMustHaveAny([C.upFlying, C.upMonumental])  # 88
	TModifierMultiplyDealtDamageComponent.new().CreateGrouped(Entity, [0]).MustNotHave([C.dtSpell]).CheckWelaConstraint().SetValueGroup([1])  # 93
	TWelaTargetConstraintUnitPropertyComponent.new().CreateGrouped(Entity, [1]).MustHaveAny([C.upFlying, C.upMonumental])  # 97
