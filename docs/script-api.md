# Script API used by the transpiled scripts

Generated by `tools/transpile_scripts.py`; do not edit. The phase 2 work list: every member of an
original class that some script calls (`M()`), reads (`M`) or assigns (`M :=`), under the class that
declares it, with the sides that use it (C = client, S = server). A `?` owner means the receiver's
type was not known statically; the member exists on all the listed classes with the same spelling.
Delphi overloads share one GDScript method: see the declarations for their parameter lists.

231 classes are referenced by name; 217 declare used members.

## TAbilitybuttonComponent (BaseConflict.EntityComponents.Client.GUI.pas:47)

`IsMultiMode()` C

## TAnimationComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1022)

`AbilityGroup()` C, `AlternatingAttack()` C, `CreateGrouped()` C, `HasAntiAirAttack()` C, `IsLink()` C, `SecondAttackGroup()` C, `SetAnimationSpeed()` C

## TAnimatorComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1334)

`ActivateOnDie()` C, `ActivateOnFire()` C, `ActivateOnLose()` C, `Duration()` C, `FreeGroupAfter()` C, `HideAfter()` C, `HideUntil()` C, `PlayAnimation()` C, `PositionKeypointsX()` C, `PositionKeypointsY()` C, `PositionKeypointsZ()` C, `PositionTimes()` C, `SizeKeypoints()` C, `SizeKeypointsXZ()` C, `SizeKeypointsY()` C, `SizeTimes()` C

## TAutoBrainBilateralComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:489)

`CheckSelfForTargetsInGroup()` S, `FireSelfInGroup()` S, `FireTargetsInGroup()` S

## TAutoBrainBuffComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:768)

`CreateGrouped()` S

## TAutoBrainComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:465)

`CreateGrouped()` S, `FireAtCommander()` S, `FireAtGround()` S, `FireAtSelf()` S, `FireAtTarget()` S, `FireInGroup()` S

## TAutoBrainOnBeforeDeath (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:645)

`CreateGrouped()` S

## TAutoBrainOnCommanderAbilityUsedComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:729)

`ConstraintOnInWelaRange()` S, `ConstraintOnSameTeamID()` S

## TAutoBrainOnDealDamageComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:670)

`AddAmountAtWrite()` S, `DontFire()` S, `RedirectToSource()` S, `WriteAmountTo()` S

## TAutoBrainOnDeathComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:628)

`FireAtKiller()` S

## TAutoBrainOnGameEventComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:591)

`SetEvent()` S

## TAutoBrainOnHealedComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:701)

`TimesForEach()` S

## TAutoBrainOnResourceComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:715)

`TimesForEach()` S, `TriggerOn()` S

## TAutoBrainOnTakeDamageComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:654)

`ModifiesAmount()` S, `TriggersAfterDamage()` S

## TAutoBrainOnUnitPropertyComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:744)

`TriggerOn()` S

## TAutoBrainWelaTargetProducedUnitComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:520)

`FireOnlyAtUnitsInOwnGroup()` S

## TBlackboard (BaseConflict.Entity.pas:1926)

`SetIndexedValue()` CS, `SetValue()` CS

## TBrainCapturePointComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.Special.pas:31)

`CreateGrouped()` S, `SetIdleGroup()` S, `SetTeamGroup()` S

## TBrainComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:168)

`ThinksInExile()` S, `ThinksLocal()` S, `ThinksPassively()` S, `ThinksPassivelyIfConscious()` S

## TBrainFleeComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:286)

`Range()` S

## TBrainProjectileComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:429)

`Bounces()` S, `CantBeReflected()` S, `NoTargetChecks()` S, `SetNotFollowingTarget()` S

## TBrainSpawnerComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.Special.pas:50)

`ApplyGridOffset()` S

## TBrainTargetingWelaComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:919)

`CreateGrouped()` S, `Preemptive()` S

## TBrainWelaCommanderComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:857)

`OverrideTargetToOwner()` S

## TBrainWelaComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:330)

`Blocking()` S, `CreateGrouped()` S

## TBrainWelaFightComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:962)

`ChangeTargetToMyself()` S, `DisableTargetLock()` S

## TBrainWelaLinkComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:979)

`CreateGrouped()` S, `LinkTime()` S, `SetBuildCheckGroup()` S

## TBrainWelaSavedTargetComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:360)

`CreateGrouped()` S, `FireAtIndex()` S

## TBuffTakenDamageMultiplierComponent (GameServer/BaseConflict.EntityComponents.Server.pas:61)

`ApplyOnHeal()` S, `DamageTypeMustHave()` S, `DamageTypeMustHaveAny()` S, `DamageTypeMustNotHave()` S, `DodgeDamage()` S

## TBuildZone (BaseConflict.Map.pas:39)

`Block()` CS, `Create()` CS, `SetFront()` CS, `SetPosition()` CS, `SetSize()` CS, `SetSpawnTarget()` CS, `SetTeam()` CS

## TBuildZoneManager (BaseConflict.Map.pas:115)

`AddBuildZone()` CS

## TCameraShakerComponent (BaseConflict.EntityComponents.Client.Visuals.pas:56)

`ActivateNow()` C, `ActivateOnCreate()` C, `ActivateOnDie()` C, `ActivateOnFire()` C, `ActivateOnFree()` C, `ActivateOnLose()` C, `CreateGrouped()` C, `Delay()` C, `Duration()` C, `Global()` C, `Invert()` C, `NoFade()` C, `PresetRotationLight()` C, `PresetRotationMedium()` C, `PresetRotationStrong()` C, `PresetVectorLight()` C, `PresetVectorMedium()` C, `PresetVectorStrong()` C, `RotationShaker()` C, `StopOnFree()` C, `Strength()` C, `VectorShaker()` C, `Waves()` C

## TCardInfo (BaseConflict.Constants.Cards.pas:87)

`Filename` CS, `League` CS, `Level` CS, `SkinID` CS

## TClickCollisionComponent (BaseConflict.EntityComponents.Client.pas:61)

`LowPriority()` C, `SetCapsule()` C

## TClientGame (BaseConflict.Game.Client.pas:41)

`ClientMap` C

## TClientMap (BaseConflict.Map.Client.pas:80)

`AddDecoEntity()` C

## TCollisionComponent (BaseConflict.EntityComponents.Shared.pas:474)

`Create()` CS

## TCommanderAbility (GameServer/BaseConflict.EntityComponents.Server.pas:473)

`CardInfo()` S, `ChargeGroup()` S, `IsMultiMode()` S

## TCommanderAbilityButtonComponent (BaseConflict.EntityComponents.Client.GUI.pas:68)

`UID()` C

## TCommanderIncomeDefaultComponent (BaseConflict.EntityComponents.Shared.pas:543)

`CreateGrouped()` CS

## TCommanderIncomeLoanComponent (BaseConflict.EntityComponents.Shared.pas:553)

`Duration()` CS, `Factor()` CS

## TCommanderIncomeOverflowComponent (BaseConflict.EntityComponents.Shared.pas:569)

`CreateGrouped()` CS

## TDeckCardButtonComponent (BaseConflict.EntityComponents.Client.GUI.pas:79)

`CardInfo()` C, `SetCooldownGroup()` C, `Slot()` C

## TDynamicZoneEmitterComponent (BaseConflict.EntityComponents.Shared.pas:581)

`SetZone()` CS

## TEntity (BaseConflict.Entity.pas:323)

`BalanceInt()` CS, `Blackboard` CS, `CardLeague()` CS, `CardLevel()` CS, `CollisionRadius` C, `CollisionRadius :=` CS, `ColorIdentity()` C, `Eventbus` CS, `HasDamageType()` CS, `ReadCollisionRadius()` C, `RemoveGroups()` S, `ReserveFreeGroup()` CS, `SkinFileSuffix()` C, `SkinID` CS, `UID :=` S

## TEntityComponent (BaseConflict.Entity.pas:211)

`Create()` CS, `CreateGrouped()` CS, `CreateGroupedAll()` S

## TEntityDisplayComponent (BaseConflict.EntityComponents.Client.GUI.pas:171)

`Create()` C, `OrderValue()` C, `SizeY()` C

## TEventbus (BaseConflict.Entity.pas:2006)

`Trigger()` S, `Write()` C, `WriteGrouped()` C

## TGame (BaseConflict.Game.pas:47)

`CharmCountCap` CS, `CharmCountCap :=` CS, `GadgetCountCap` CS, `GadgetCountCap :=` CS, `GameDirector` CS, `GoldCap` CS, `GoldCap :=` CS, `GoldCapPerTier` S, `GoldCapPerTier :=` CS, `HasShowdown()` CS, `IncomeRatePerIncomeUpgrade` CS, `IncomeRatePerIncomeUpgrade :=` CS, `IncomeUpgradeCap` CS, `IncomeUpgradeCap :=` CS, `IncomeUpgradeCostPerIncomeUpgrade` CS, `IncomeUpgradeCostPerIncomeUpgrade :=` CS, `IsDuo()` CS, `IsOneLane()` CS, `IsPvP()` CS, `IsTwoLane()` CS, `League()` CS, `Map` CS, `StartingGold` CS, `StartingGold :=` CS, `StartingIncomeRate` CS, `StartingIncomeRate :=` CS, `StartingIncomeUpgradeCost` CS, `StartingIncomeUpgradeCost :=` CS, `StartingTier` CS, `StartingTier :=` CS, `StartingWood` CS, `StartingWood :=` CS

## TGameDirectorComponent (BaseConflict.EntityComponents.Shared.pas:59)

`AddEvent()` CS, `AddEventIf()` CS

## TGameEventEnumeratorComponent (BaseConflict.EntityComponents.Shared.pas:630)

`Event()` CS

## THealthComponent (BaseConflict.EntityComponents.Shared.pas:173)

`Create()` S

## TIndicatorCooldownCircleComponent (BaseConflict.EntityComponents.Client.GUI.pas:474)

`Color()` C, `Create()` C, `CreateGrouped()` C, `Invert()` C, `InvertDirection()` C, `Offset()` C, `Opacity()` C, `ScaleWith()` C, `SetLinearBlend()` C, `SetRadius()` C, `SetTexture()` C, `SetThickness()` C, `ShowAsTeam()` C, `ShowsGameEvent()` C, `ShowsResource()` C

## TIndicatorResourceCircleComponent (BaseConflict.EntityComponents.Client.GUI.pas:523)

`CreateGrouped()` C, `Padding()` C

## TIndicatorShowTextComponent (BaseConflict.EntityComponents.Client.GUI.pas:573)

`Clamp()` C, `Color()` C, `ColorAtCap()` C, `CreateGrouped()` C, `CursorOffset()` C, `FixedCap()` C, `NoCap()` C, `ResourceFromCommander()` C, `ResourceGroup()` C, `ShowResource()` C, `SpelltargetVisualizer()` C

## TLaneManager (BaseConflict.Map.pas:169)

`single()` CS

## TLinkBrainComponent (GameServer/BaseConflict.EntityComponents.Server.pas:150)

`CreateGrouped()` S, `FiresAtCreate()` S

## TLinkBulletVisualizerComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1164)

`Color()` C, `Create()` C, `ImpactParticleEffect()` C, `ImpactSize()` C, `SetBulletSpawnCooldown()` C, `SetEndposJitter()` C, `SetLength()` C, `SetSpeed()` C, `SetWidth()` C, `ToGround()` C

## TLinkEffectDamageRedirectionComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:703)

`DestinationToSource()` S

## TLinkRayVisualizerComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1275)

`CreateGrouped()` C, `SetDestSubPosition()` C

## TMap (BaseConflict.Map.pas:197)

`BuildZones` CS, `Lanes` CS

## TMeshComponent (BaseConflict.EntityComponents.Client.Visuals.pas:870)

`AlternatingZone()` C, `ApplyAutoSizeNormalization()` C, `ApplyLegacySizeFactor()` C, `BindTextureToResource()` C, `BindTextureToTeam()` C, `BindTextureToUnitProperty()` C, `BindZoneToBone()` C, `BoneInvertX()` C, `BoneInvertY()` C, `BoneInvertZ()` C, `BoneOffset()` C, `BoneRotation()` C, `BoneSwapXY()` C, `BoneSwapXZ()` C, `BoneSwapYZ()` C, `BoneSwizzleXZY()` C, `BoneSwizzleYZX()` C, `CastsNoShadows()` C, `Create()` C, `CreateGrouped()` C, `CreateNewAnimation()` C, `DeathColorIdentityOverride()` C, `HasAttackLoop()` C, `IgnoreScalingForAnimations()` C, `IsDecal()` C, `IsEffectMesh()` C, `NoWalkOffset()` C, `SetAnimationSpeed()` C, `SetFollowingDefaultAnimation()` C

## TMeshEffect (BaseConflict.EntityComponents.Client.Visuals.pas:472)

`Additive()` C, `AssignToEntity()` C, `OverrideColorIdentity()` C

## TMeshEffectComponent (BaseConflict.EntityComponents.Client.Visuals.pas:989)

`ActivateOnDie()` C, `ActivateOnFire()` C, `ActivateOnLose()` C, `ActivateOnPreFire()` C, `ActivateOnWelaUnitProduced()` C, `ApplyToFireTarget()` C, `CreateGrouped()` C, `SetEffect()` C, `TargetGroup()` C

## TMeshEffectGeneric (BaseConflict.EntityComponents.Client.Visuals.pas:511)

`SetTexture()` C

## TMeshEffectGhost (BaseConflict.EntityComponents.Client.Visuals.pas:618)

`Additive()` C, `Color()` C, `Create()` C

## TMeshEffectGlow (BaseConflict.EntityComponents.Client.Visuals.pas:637)

`Create()` C, `FixedColorIdentity()` C

## TMeshEffectHideAndGlow (BaseConflict.EntityComponents.Client.Visuals.pas:652)

`Create()` C

## TMeshEffectIce (BaseConflict.EntityComponents.Client.Visuals.pas:569)

`Create()` C

## TMeshEffectInvisible (BaseConflict.EntityComponents.Client.Visuals.pas:741)

`Create()` C, `Smooth()` C, `Span()` C, `Speed()` C

## TMeshEffectMatcap (BaseConflict.EntityComponents.Client.Visuals.pas:719)

`Create()` C

## TMeshEffectMetal (BaseConflict.EntityComponents.Client.Visuals.pas:706)

`Create()` C, `ShowAsColor()` C

## TMeshEffectSoulGain (BaseConflict.EntityComponents.Client.Visuals.pas:602)

`Color()` C, `Create()` C, `Radius()` C

## TMeshEffectSpawn (BaseConflict.EntityComponents.Client.Visuals.pas:796)

`Create()` C, `Legendary()` C, `OverrideColor()` C, `OverrideEffectTime()` C

## TMeshEffectSpherify (BaseConflict.EntityComponents.Client.Visuals.pas:663)

`Create()` C, `PowFactor()` C, `SetFixedCenter()` C

## TMeshEffectStone (BaseConflict.EntityComponents.Client.Visuals.pas:581)

`Create()` C

## TMeshEffectTint (BaseConflict.EntityComponents.Client.Visuals.pas:554)

`Additive()` C, `Create()` C

## TMeshEffectVoid (BaseConflict.EntityComponents.Client.Visuals.pas:756)

`Create()` C

## TMeshEffectWarp (BaseConflict.EntityComponents.Client.Visuals.pas:693)

`Create()` C, `Smooth()` C

## TMeshEffectWithTimekeys (BaseConflict.EntityComponents.Client.Visuals.pas:525)

`AddKey()` C, `AddNextTimeLine()` C, `AddPermaKey()` C

## TMeshEffectWobble (BaseConflict.EntityComponents.Client.Visuals.pas:730)

`Create()` C

## TMinimapPingComponent (BaseConflict.EntityComponents.Client.GUI.pas:130)

`Size()` C, `Texture()` C

## TModifierArmorTypeComponent (BaseConflict.EntityComponents.Shared.Wela.pas:134)

`Increase()` CS, `SetTo()` CS

## TModifierBlindedComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:754)

`SetFireGroup()` S

## TModifierComponent (BaseConflict.EntityComponents.Shared.Wela.pas:32)

`CreateGrouped()` CS, `ReadyGroup()` CS, `SetValueGroup()` CS

## TModifierCostComponent (BaseConflict.EntityComponents.Shared.Wela.pas:207)

`ScaleWithResource()` CS

## TModifierDamageTypeComponent (BaseConflict.EntityComponents.Shared.Wela.pas:45)

`Add()` CS

## TModifierMultiplyDealtDamageComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:737)

`CheckWelaConstraint()` S, `MustNotHave()` S

## TModifierResourceComponent (BaseConflict.EntityComponents.Shared.Wela.pas:102)

`AddModifier()` S, `ApplyNow()` S, `DontFillCap()` S, `Resource()` S, `ScaleWithResource()` S, `UseResourceCap()` S

## TModifierWelaDamageComponent (BaseConflict.EntityComponents.Shared.Wela.pas:151)

`Divide()` CS, `FactorForUnitProperty()` CS, `Multiply()` CS, `ResourceGroup()` CS, `ScaleWithResource()` CS

## TModifierWelaRangeComponent (BaseConflict.EntityComponents.Shared.Wela.pas:176)

`ActivateOnStand()` CS, `AddModifier()` CS, `DeactivateOnMoveTo()` CS, `ScaleWithStage()` CS, `ScaleWithTime()` CS

## TModifierWelaTargetCountComponent (BaseConflict.EntityComponents.Shared.Wela.pas:78)

`ScaleWithResource()` S

## TMovementComponent (BaseConflict.EntityComponents.Shared.pas:108)

`Create()` S

## TObject (System.pas:0)

`Create()` C, `Free()` C

## TOrienterAutoRotationComponent (BaseConflict.EntityComponents.Client.Visuals.pas:193)

`CreateGrouped()` C, `RandomOffset()` C, `SetSpeed()` C, `SpeedJitter()` C, `StopAt()` C

## TOrienterFrontComponent (BaseConflict.EntityComponents.Client.Visuals.pas:165)

`Front()` C

## TOrienterSmoothRotateComponent (BaseConflict.EntityComponents.Client.Visuals.pas:178)

`SetAngleSpeed()` C

## TOrienterTargetComponent (BaseConflict.EntityComponents.Client.Visuals.pas:138)

`FrontFromPivot()` C, `FrontGroup()` C, `FrontWithY()` C, `KeepLastFront()` C, `TargetGroup()` C

## TParticleEffectComponent (BaseConflict.EntityComponents.Client.Visuals.pas:342)

`ActivateAtFireTarget()` C, `ActivateNow()` C, `ActivateOnCreate()` C, `ActivateOnDie()` C, `ActivateOnFire()` C, `ActivateOnFireDelayed()` C, `ActivateOnFireWarhead()` C, `ActivateOnFree()` C, `ActivateOnLose()` C, `ActivateOnPreFire()` C, `ActivateOnWelaActivate()` C, `ClonesToTarget()` C, `Create()` C, `CreateGrouped()` C, `DeactivateOnFire()` C, `DeactivateOnTime()` C, `Delay()` C, `EmitFromAllBones()` C, `FixedHeightGround()` C, `FixedOrientationDefault()` C, `IgnoreModelSize()` C, `PlaceAtSavedTarget()` C, `ScaleWith()` C

## TPathfindingComponent (BaseConflict.EntityComponents.Shared.pas:497)

`Create()` CS

## TPointLightComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1069)

`Create()` C, `CreateGrouped()` C, `Negate()` C, `SetLightShape()` C

## TPositionerAttacherComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1291)

`ApplyFront()` C, `AttachToDestination()` C, `AttachToSource()` C, `CreateGrouped()` C, `Offset()` C, `SetSubPosition()` C, `SetSubPositionGroup()` C

## TPositionerOffsetComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1403)

`CreateGrouped()` C, `Offset()` C, `OverrideY()` C, `TargetGroup()` C, `Transition()` C

## TPositionerRotationComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1450)

`CreateGrouped()` C, `Factor()` C, `FadeIn()` C, `Phase()` C, `Radius()` C, `RadiusFromEvent()` C, `RotationSpeed()` C, `TargetGroup()` C

## TPositionerSplineComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1478)

`BindToSubPosition()` C, `BindToTargetSubPosition()` C, `BothTangents()` C, `BothTangentsAfterReached()` C, `EndTangent()` C, `MaxDistanceScaling()` C, `OrientStartWithTarget()` C, `StartOffset()` C, `StartTangent()` C, `StartTangentRandom()` C

## TProductionPreviewComponent (BaseConflict.EntityComponents.Client.Visuals.pas:424)

`CreateGrouped()` C, `IsSpawner()` C

## TRangeIndicatorComponent (BaseConflict.EntityComponents.Client.GUI.pas:383)

`CreateGrouped()` C, `IsPermanent()` C

## TRangeIndicatorHighlightEntitiesComponent (BaseConflict.EntityComponents.Client.GUI.pas:444)

`Invalid()` C, `Valid()` C

## TResourceDisplayBarComponent (BaseConflict.EntityComponents.Client.GUI.pas:274)

`GradientBottom()` C, `GradientTop()` C

## TResourceDisplayComponent (BaseConflict.EntityComponents.Client.GUI.pas:244)

`Create()` C, `HideIfEmpty()` C, `ShowResource()` C

## TResourceDisplayHealthComponent (BaseConflict.EntityComponents.Client.GUI.pas:316)

`Create()` C

## TResourceDisplayIntegerProgressBarComponent (BaseConflict.EntityComponents.Client.GUI.pas:341)

`FixedCap()` C, `NoCap()` C

## TScenarioDirectorComponent (GameServer/BaseConflict.EntityComponents.Server.pas:797)

`AddKIPlayer()` S, `AddUnitsToSpawnerSubset()` S, `ChangeGoldIncome()` S, `ChangeUnitSpawnerSubset()` S, `ChangeWood()` S, `ChangeWoodIncome()` S, `ChooseUnitFaction()` S, `Create()` S, `DisableMirroring()` S, `EnableMirroring()` S, `RegisterBossWave()` S, `RegisterBossWaveAtTime()` S, `SetLeague()` S, `SetTeam()` S, `SpawnGuards()` S, `SpawnRandomBossWave()` S, `SpawnUnit()` S, `SpawnUnitWithoutLimitedLifetime()` S, `SpawnUnitWithoutLimitedLifetimeAndReturnEntity()` S, `SpawnUnitWithoutLimitedLifetimeWithFront()` S, `UnregisterBossWaveAtTime()` S

## TServerCardPlayStatisticsComponent (GameServer/BaseConflict.EntityComponents.Server.pas:330)

`CreateGrouped()` S

## TServerEntityManagerComponent (GameServer/BaseConflict.EntityComponents.Server.pas:358)

`SpawnUnit()` S, `SpawnUnitWithOverwatch()` S, `SpawnUnitWithOverwatchAndFlee()` S

## TServerGame (GameServer/BaseConflict.Game.Server.pas:93)

`ScenarioDirector` S, `ScenarioDirector :=` S, `ServerEntityManager` S

## TShowOnMinimapComponent (BaseConflict.EntityComponents.Client.GUI.pas:681)

`Create()` C

## TSoundComponent (BaseConflict.EntityComponents.Client.Sound.pas:46)

`BindParameterToResource()` C, `CheckGroup()` C, `CheckOption()` C, `Create()` C, `CreateGrouped()` C, `Delay()` C, `IsLoop()` C, `IsPiece()` C, `StopGroup()` C, `StopOnFire()` C, `StopOnFree()` C, `TriggerNow()` C, `TriggerOnCreate()` C, `TriggerOnDie()` C, `TriggerOnFire()` C, `TriggerOnFireWarhead()` C, `TriggerOnFree()` C, `TriggerOnLose()` C, `TriggerOnLoseHealth()` C, `TriggerOnPreFire()` C, `UsePositionOfTarget()` C

## TSpelltargetVisualizerLineBetweenTargetsComponent (BaseConflict.EntityComponents.Client.GUI.pas:631)

`CreateGrouped()` C, `Texture()` C, `Width()` C

## TSpelltargetVisualizerShowPatternComponent (BaseConflict.EntityComponents.Client.GUI.pas:616)

`CreateGrouped()` C, `ShowForIndex()` C

## TSpelltargetVisualizerShowTextureComponent (BaseConflict.EntityComponents.Client.GUI.pas:540)

`Additive()` C, `CreateGrouped()` C, `NoValidChecks()` C, `SetSize()` C, `SetTexture()` C

## TStateDisplayComponent (BaseConflict.EntityComponents.Client.GUI.pas:224)

`CheckWelaIsReadyInGroup()` C, `ReverseWelaIsReadyCheck()` C, `Texture()` C

## TTextureRangeIndicatorComponent (BaseConflict.EntityComponents.Client.GUI.pas:406)

`Cone()` C, `CreateGrouped()` C, `DrawCircle()` C, `DrawOnShowSpawnZone()` C, `HideInCaptureMode()` C, `Offset()` C, `Opacity()` C, `SetTexture()` C, `ShowTeamColor()` C, `ShowWeaponRange()` C, `Size()` C, `Slice()` C

## TThinkImpulseFireComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:57)

`TargetGroup()` S

## TThinkImpulseNowComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:37)

`CreateGrouped()` S

## TThinkImpulseOnceComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:86)

`TriggerOnDeploy()` S, `WaitOneFrame()` S

## TThinkImpulseTimerComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:111)

`CreateGrouped()` S

## TThinkImpulseTimerCooldownComponent (GameServer/BaseConflict.EntityComponents.Server.Brains.pas:130)

`CreateGrouped()` S, `Once()` S, `TimerIsReady()` S

## TTooltipUnitAbilityComponent (BaseConflict.EntityComponents.Client.GUI.pas:100)

`Create()` C, `CreateGrouped()` C, `IsCardDescription()` C, `Keyword()` C, `PassInteger()` C, `PassPercentage()` C, `PassSingle()` C, `PassSingleAsInteger()` C, `PassString()` C

## TTutorialDirectorActionArrowHighlight (BaseConflict.EntityComponents.Client.pas:718)

`Create()` C, `Element()` C, `ElementAnchor()` C, `LoadElementFromGroup()` C, `NoBackdrop()` C, `Text()` C, `WindowAnchor()` C, `WindowButton()` C, `WorldPoint()` C, `WorldRadius()` C

## TTutorialDirectorActionCamera (BaseConflict.EntityComponents.Client.pas:764)

`SpawnerJumpAllowed()` C

## TTutorialDirectorActionGUIElement (BaseConflict.EntityComponents.Client.pas:686)

`AddClass()` C, `Element()` C, `RemoveClass()` C

## TTutorialDirectorActionMoveCamera (BaseConflict.EntityComponents.Client.pas:785)

`FollowLastSpawnedEntity()` C, `Time()` C

## TTutorialDirectorActionScript (BaseConflict.EntityComponents.Client.pas:649)

`Create()` C, `Entity()` C, `Scriptfile()` C

## TTutorialDirectorActionTutorialHint (BaseConflict.EntityComponents.Client.pas:673)

`ButtonText()` C, `Text()` C

## TTutorialDirectorActionWorld (BaseConflict.EntityComponents.Client.pas:635)

`Create()` C, `Position()` C, `Size()` C, `Up()` C

## TTutorialDirectorActionWorldTexture (BaseConflict.EntityComponents.Client.pas:742)

`TexturePath()` C

## TTutorialDirectorComponent (BaseConflict.EntityComponents.Client.pas:836)

`AddActions()` C, `AddStepCamPos()` C, `AddStepClientInit()` C, `AddStepGameEvent()` C, `AddStepGameTick()` C, `AddStepNever()` C, `AddStepNewEntity()` C, `AddStepTimer()` C, `AddStepUIDsDead()` C, `AllowMultiCardPlay()` C, `BuildElementGroup()` C, `BuildKeybindingGroup()` C, `ClearGroupLock()` C, `ClearGroupStates()` C, `ClearTutorialHint()` C, `ClearWorldObjects()` C, `Create()` C, `GotoStep()` C, `GroundText()` C, `HideGroup()` C, `IfGameEventGotoStep()` C, `IfLastEntityDiesGotoStep()` C, `IfTimerGotoStep()` C, `LockCamera()` C, `LockGroup()` C, `MoveCamera()` C, `MoveCameraOverTime()` C, `PassiveText()` C, `PreventMultiCardPlay()` C, `SendGameevent()` C, `StepLabel()` C, `StepTriggerCount()` C, `UnlockCamera()` C, `UnlockGroup()` C

## TUnitPropertyComponent (BaseConflict.EntityComponents.Shared.pas:36)

`CreateGrouped()` CS, `GivePropertyOwner()` CS, `Remove()` CS

## TVertexQuadComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1085)

`Additive()` C, `CameraOriented()` C, `Color()` C, `Create()` C, `ScreenSpace()` C

## TVertexRayVisualizerComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1210)

`BindEndOffset()` C, `BindStartOffset()` C, `BindStartToSubPositionGroup()` C, `CreateGrouped()` C, `Planar()` C, `ScaleWidth()` C, `ScaleWith()` C, `SetEndposJitter()` C, `SetLength()` C, `SetLongitudinalSpeed()` C, `SetOpacity()` C, `SetRaycount()` C, `SetRotationSpeed()` C, `SetStartWidth()` C, `SetWidth()` C, `SizeKeypointsLength()` C, `SizeKeypointsWidth()` C, `SizeTimes()` C, `Texture()` C

## TVertexTraceComponent (BaseConflict.EntityComponents.Client.Visuals.pas:1117)

`ActivateNow()` C, `ActivateOnFire()` C, `Additive()` C, `Color()` C, `CreateGrouped()` C, `DeactivateAfterTime()` C, `DeactivateOnMoveTo()` C, `FadeLength()` C, `FadeWidening()` C, `LocalSpace()` C, `MaxLength()` C, `RollUpSpeed()` C, `SamplingDistance()` C, `Texture()` C, `TexturePerDistance()` C, `Width()` C

## TVisualizerComponent (BaseConflict.EntityComponents.Client.Visuals.pas:257)

`BindToSubPosition()` C, `BindToSubPositionGroup()` C, `FixedHeightGround()` C, `FixedOrientation()` C, `FixedOrientationAngle()` C, `FixedOrientationDefault()` C, `IgnoreModelSize()` C, `IgnoreSize()` C, `IsPiece()` C, `MaxScale()` C, `ScaleWith()` C, `ScaleWithResource()` C, `SetModelOffset()` C, `SetModelRotationOffset()` C, `ShowAsTeam()` C, `VisibleWithOption()` C, `VisibleWithResource()` C, `VisibleWithUnitPropertyMustHave()` C, `VisibleWithUnitPropertyMustNotHave()` C, `VisibleWithWelaReady()` C, `VisibleWithWelaReadyGrouped()` C

## TVisualModificatorSizeComponent (BaseConflict.EntityComponents.Client.Visuals.pas:227)

`Duration()` C, `Keypoints()` C, `KeypointsY()` C, `SetTargetSize()` C, `TimeKeys()` C

## TWarheadApplyScriptComponent (BaseConflict.EntityComponents.Shared.Wela.pas:855)

`ApplyToProducedUnits()` CS, `ApplyToSelfAtCreate()` CS, `CreateGrouped()` CS, `Methodname()` CS, `OverrideLastParameterGroup()` CS, `PassDirectionToTarget()` CS, `PassIntValue()` CS, `PassOffsetToOwner()` CS, `PassResource()` CS, `PassSameTeam()` CS, `PassSavedTargetPosition()` CS, `PassValueFromEvent()` CS

## TWarheadComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:29)

`RedirectToSelf()` S

## TWarheadLinkApplyScriptComponent (BaseConflict.EntityComponents.Shared.Wela.pas:918)

`CreateGrouped()` CS

## TWarheadSplashComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:222)

`CreateGrouped()` S, `IgnoreMainTargets()` S, `LineFromOwner()` S, `SetValidateGroup()` S, `TargetsGroundAndAir()` S

## TWarheadSplashHealComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:276)

`CreateGrouped()` S

## TWarheadSpottyHealComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:136)

`CreateGrouped()` S

## TWarheadSpottyHealthComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:118)

`PercentageOfMaxHealth()` S

## TWarheadSpottyKillComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:91)

`Exile()` S, `Remove()` S, `Sacrifice()` S

## TWarheadSpottyRemoveBuffComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:104)

`All()` S, `MustHaveAny()` S, `MustNotHave()` S

## TWarheadSpottyResourceComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:159)

`AmountIsPercentage()` S, `ChangesMax()` S, `CreateGrouped()` S, `DontFillCap()` S, `ResetResource()` S, `SearchForWelaBeacon()` S, `SetFactor()` S, `SetResourceSource()` S, `SetResourceType()` S, `SetsResourceToValue()` S, `TargetGroup()` S

## TWarheadSpottyTeleportComponent (GameServer/BaseConflict.EntityComponents.Server.Warheads.pas:66)

`AsProjectile()` S, `CreateGrouped()` S, `Offset()` S, `OffsetByCollisionRadius()` S, `ToCoordinate()` S, `ToNexus()` S

## TWelaEffectActivationAbilityComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:353)

`CheckNotFull()` S, `CreateGrouped()` S, `OnlyAfterGameStart()` S, `SetActivationGroup()` S, `SetCheckGroup()` S, `SetsActive()` S, `TriggerOnReachResourceCap()` S

## TWelaEffectFactoryComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:411)

`CreateGrouped()` S, `IsSpawner()` S, `PassCardValues()` S, `PassTargets()` S, `SetSpawnedTeam()` S, `SpreadSpawns()` S

## TWelaEffectFireComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:494)

`FireInCreator()` S, `MultiTargetGroup()` S, `RandomizeGroundtarget()` S, `RedirectToGround()` S, `RedirectToSelf()` S, `TargetGroup()` S

## TWelaEffectGameEventComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:482)

`Event()` S

## TWelaEffectIncreaseResourceComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:456)

`SetResourceType()` S

## TWelaEffectInstantComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:467)

`CreateGrouped()` S, `TargetGroup()` S

## TWelaEffectLinkPayCostMyselfComponentServer (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:613)

`FireOnEmpty()` S

## TWelaEffectPayCostComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:306)

`ConsumesAll()` S, `ConvertResource()` S, `CreateGrouped()` S, `SetPayingGroup()` S, `SetPayingGroupForType()` S

## TWelaEffectProjectileComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:549)

`Reverse()` S

## TWelaEffectRedirecterComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:253)

`RedirectToGround()` S

## TWelaEffectRemoveAfterUseComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:397)

`TargetGroup()` S

## TWelaEffectRemoveBeaconComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:537)

`SearchForWelaBeacon()` S

## TWelaEffectReplaceComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:337)

`KeepResource()` S, `KeepTakenDamage()` S, `SetNewTeam()` S

## TWelaEffectResetCooldownComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:522)

`Expire()` S, `SearchForWelaBeacon()` S, `TargetGroup()` S

## TWelaEffectStatisticsComponent (GameServer/BaseConflict.EntityComponents.Server.Statistics.pas:60)

`CheckDamageType()` S, `CheckMaxTargets()` S, `CheckNth()` S, `CheckResourceEmpty()` S, `CheckResourceMax()` S, `CheckUnitPropertyMustHave()` S, `CheckUnitPropertyMustHaveAny()` S, `Name()` S, `TakeOwnerFromTarget()` S, `TriggerGlobalOnKillDone()` S, `TriggerOnCreate()` S, `TriggerOnDamageDone()` S, `TriggerOnDamageDoneTargets()` S, `TriggerOnDie()` S, `TriggerOnDuration()` S, `TriggerOnFire()` S, `TriggerOnHeal()` S, `TriggerOnHealDone()` S, `TriggerOnKill()` S, `TriggerOnKillDone()` S, `TriggerOnKilled()` S, `TriggerOnTakeDamage()` S

## TWelaEffectSuicideComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:372)

`PreventDeath()` S

## TWelaEffectWaveSpawnComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.Special.pas:58)

`CreateGrouped()` S

## TWelaEfficiencyDamageTypeComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:865)

`Prioritize()` S

## TWelaEfficiencyMaxHealthComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:854)

`Inverse()` S

## TWelaEfficiencyUnitPropertyComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:876)

`Prioritize()` S, `Reverse()` S

## TWelaEventRedirecter (BaseConflict.EntityComponents.Shared.Wela.pas:832)

`CopyIndexedValue()` CS, `CopyValue()` CS

## TWelaHelperActivateTimerComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:811)

`CreateGrouped()` S, `Delay()` S

## TWelaHelperBeaconComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:781)

`TriggerAt()` S

## TWelaHelperInitActiveAfterGameStartComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:794)

`CreateGrouped()` S, `DisableOnReachResourceCap()` S

## TWelaHelperResolveComponent (BaseConflict.EntityComponents.Shared.Wela.pas:941)

`ResolveCurrentTier()` S, `ResolveTeamID()` S, `ResolveTier()` CS

## TWelaLinkEffectComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:570)

`CreateGrouped()` S, `CreatorGroup()` S

## TWelaLinkEffectUnitPropertyComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:643)

`CreateGrouped()` S

## TWelaReadyAfterGameEventComponent (BaseConflict.EntityComponents.Shared.Wela.pas:711)

`GameEvent()` S

## TWelaReadyCooldownComponent (BaseConflict.EntityComponents.Shared.Wela.pas:767)

`Cooldown()` S, `CreateGrouped()` CS, `Once()` S

## TWelaReadyCostComponent (BaseConflict.EntityComponents.Shared.Wela.pas:605)

`CostsCap()` CS, `CreateGrouped()` CS, `SetPayingGroup()` CS, `SetPayingGroupForType()` CS, `ValueGroup()` C

## TWelaReadyEnemiesNearbyComponent (BaseConflict.EntityComponents.Shared.Wela.pas:685)

`CheckGroup()` C, `CreateGrouped()` C, `ValueGroup()` C

## TWelaReadyEntityNearbyComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:234)

`CreateGrouped()` S, `ReadyIfNoTargets()` S, `TargetingGroup()` S

## TWelaReadyEventCompareComponent (BaseConflict.EntityComponents.Shared.Wela.pas:747)

`ComparedEvent()` S, `CreateGrouped()` S, `ReferenceValue()` S, `SetComparator()` S

## TWelaReadyNthComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:215)

`Nth()` S, `Times()` S

## TWelaReadyResourceCompareComponent (BaseConflict.EntityComponents.Shared.Wela.pas:630)

`CheckEmpty()` S, `CheckFull()` S, `CheckingGroup()` S, `CheckNotEmpty()` S, `CheckNotFull()` S, `ChecksCommander()` S, `ComparedResource()` CS, `ReferenceIsAbsolute()` CS, `ReferenceValue()` CS, `SetComparator()` CS

## TWelaReadyUnitPropertyComponent (BaseConflict.EntityComponents.Shared.Wela.pas:669)

`ChecksCommander()` CS, `MustHave()` CS, `MustNotHave()` CS

## TWelaTargetConstraintAlliesComponent (BaseConflict.EntityComponents.Shared.Wela.pas:415)

`CreateGrouped()` CS

## TWelaTargetConstraintBooleanComponent (BaseConflict.EntityComponents.Shared.Wela.pas:348)

`GroupA()` CS, `GroupB()` CS, `OperatorAnd()` CS, `OperatorOr()` CS

## TWelaTargetConstraintCardNameComponent (BaseConflict.EntityComponents.Shared.Wela.pas:428)

`AddCard()` CS

## TWelaTargetConstraintCompareUnitPropertyComponent (BaseConflict.EntityComponents.Shared.Wela.pas:469)

`BothMustHaveAny()` CS

## TWelaTargetConstraintComponent (BaseConflict.EntityComponents.Shared.Wela.pas:222)

`ConstraintsWarhead()` S

## TWelaTargetConstraintDynamicZoneComponent (BaseConflict.EntityComponents.Shared.Wela.pas:382)

`SetZone()` CS

## TWelaTargetConstraintEnemiesComponent (BaseConflict.EntityComponents.Shared.Wela.pas:420)

`CreateGrouped()` CS

## TWelaTargetConstraintEventComponent (BaseConflict.EntityComponents.Shared.Wela.pas:501)

`CreateGrouped()` CS

## TWelaTargetConstraintResourceCompareComponent (BaseConflict.EntityComponents.Shared.Wela.pas:315)

`ComparedResource()` S, `CreateGrouped()` S, `SetComparator()` S, `TargetFactor()` S

## TWelaTargetConstraintResourceComponent (BaseConflict.EntityComponents.Shared.Wela.pas:281)

`CheckFull()` S, `CheckHasResource()` CS, `CheckNotFull()` CS, `CheckResource()` CS, `Comparator()` S, `CompareCapToReference()` S, `Reference()` S

## TWelaTargetConstraintTeamIDComponent (BaseConflict.EntityComponents.Shared.Wela.pas:395)

`Invert()` S, `SetTargetTeam()` S

## TWelaTargetConstraintUnitPropertyComponent (BaseConflict.EntityComponents.Shared.Wela.pas:455)

`MustHave()` CS, `MustHaveAny()` CS, `MustNotHave()` CS

## TWelaTargetConstraintWelaPropertyComponent (BaseConflict.EntityComponents.Shared.Wela.pas:486)

`CheckGroup()` S, `CreateGrouped()` S, `MustNotHave()` S

## TWelaTargetConstraintZoneComponent (BaseConflict.EntityComponents.Shared.Wela.pas:367)

`CreateGrouped()` CS, `SetPadding()` CS

## TWelaTargetingComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:40)

`CreateGrouped()` S, `MaxNewTargetCount()` S, `PicksRandomTargets()` S, `PicksRandomTargetsWithRepetition()` S, `SetTargetTeamConstraint()` S, `SetTargetTeamConstraintPriority()` S, `SetValidateGroup()` S

## TWelaTargetingRadialAttentionComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:82)

`DisableStraightPreference()` S

## TWelaTargetingRadialComponent (GameServer/BaseConflict.EntityComponents.Server.Welas.pas:116)

`Cone()` S, `CreateGrouped()` S, `IgnoreOwnCollisionradius()` S, `PrioritizeMiddleDistant()` S, `PrioritizeMostDistant()` S, `RangeFromEvent()` S

## ? TCameraShakerComponent/TParticleEffectComponent

`ActivateOnCreate()` C, `ActivateOnFireWarhead()` C

## ? TCameraShakerComponent/TParticleEffectComponent/TVertexTraceComponent

`ActivateNow()` C

## ? TIndicatorCooldownCircleComponent/TIndicatorShowTextComponent/TLinkBulletVisualizerComponent/TMeshEffectGhost/TMeshEffectSoulGain/TTutorialDirectorActionWorld/TVertexQuadComponent/TVertexTraceComponent

`Color()` C

## ? TIndicatorCooldownCircleComponent/TParticleEffectComponent/TVertexRayVisualizerComponent/TVisualizerComponent

`ScaleWith()` C

## ? TMeshEffectWithTimekeys

`AddKey()` C

## ? TParticleEffectComponent

`ActivateAtFireTarget()` C, `ActivateOnWelaActivate()` C

## ? TParticleEffectComponent/TVisualizerComponent

`FixedHeightGround()` C, `FixedOrientationDefault()` C, `IgnoreModelSize()` C, `ScaleRange()` C

## ? TSoundComponent/TVisualizerComponent

`IsPiece()` C

## ? TVisualizerComponent

`BindToSubPositionGroup()` C, `FixedOffsetGround()` C, `IgnoreSize()` C, `MaxScale()` C, `SetModelOffset()` C, `SetModelRotationOffset()` C, `SwapXZBindMatrix()` C, `VisibleWithOption()` C
